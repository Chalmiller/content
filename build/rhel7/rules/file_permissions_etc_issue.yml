conflicts: []
description: '

  To properly set the permissions of <code>/etc/issue</code>, run the command:

  <pre>$ sudo chmod 0644 /etc/issue</pre>'
identifiers: {cce: CCE-83347-5}
inherited_platforms: [null]
ocil: 'To check the permissions of <code>/etc/issue</code>, run the command: <pre>$
  ls -l /etc/issue</pre> If properly configured, the output should indicate the following
  permissions: <code>-rw-r--r--</code>'
ocil_clause: /etc/issue has unix mode -rw-r--r--
oval_external_content: null
platform: null
prodtype: fedora,rhcos4,ol7,ol8,rhel7,rhel8,rhv4,sle15,wrlinux1019
rationale: 'Display of a standardized and approved use notification before granting

  access to the operating system ensures privacy and security notification

  verbiage used is consistent with applicable federal laws, Executive Orders,

  directives, policies, regulations, standards, and guidance.<br />

  Proper permissions will ensure that only root user can modify the banner.'
references: {cis: 1.7.1.5}
requires: []
severity: medium
template:
  backends: {}
  name: file_permissions
  vars: {filemode: '0644', filepath: /etc/issue}
title: Verify permissions on System Login Banner
warnings: []
