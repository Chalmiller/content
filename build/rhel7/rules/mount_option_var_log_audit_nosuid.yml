conflicts: []
description: 'The <tt>nosuid</tt> mount option can be used to prevent

  execution of setuid programs in <tt>/var/log/audit</tt>. The SUID and SGID permissions

  should not be required in directories containing audit log files.

  Add the <code>nosuid</code> option to the fourth column of

  <tt>/etc/fstab</tt> for the line which controls mounting of

  <code>/var/log/audit</code>.'
identifiers: {cce: CCE-82148-8}
inherited_platforms: [null]
ocil: "To verify the <tt>nosuid</tt> option is configured for the <tt>/var/log/audit</tt>\
  \ mount point, run the following command:\n    <pre>$ mount | grep '\\s/var/log/audit\\\
  s'</pre>\n    The output should show the corresponding mount point along with the\
  \ <tt>nosuid</tt> setting in parentheses.\n"
ocil_clause: the  is not present in the output line, or there is no output line at
  all
oval_external_content: null
platform: machine
prodtype: fedora,ol7,ol8,rhel7,rhel8
rationale: 'The presence of SUID and SGID executables should be tightly controlled.
  Users

  should not be able to execute SUID or SGID binaries from partitions

  designated for audit log files.'
references: {nist: 'CM-7(a),CM-7(b),CM-6(a),AC-6,AC-6(1),MP-7', nist-csf: 'PR.IP-1,PR.PT-2,PR.PT-3',
  srg: SRG-OS-000368-GPOS-00154}
requires: []
severity: medium
template:
  backends: {}
  name: mount_option
  vars: {mount_has_to_exist: 'yes', mountoption: nosuid, mountpoint: /var/log/audit}
title: Add nosuid Option to /var/log/audit
warnings: []
