conflicts: []
description: ' To properly set the owner of <code>/etc/shadow-</code>, run the command:
  <pre>$ sudo chown root /etc/shadow- </pre>'
identifiers: {cce: CCE-83412-7}
inherited_platforms: [null]
ocil: 'To check the ownership of <code>/etc/shadow-</code>, run the command: <pre>$
  ls -lL /etc/shadow-</pre> If properly configured, the output should indicate the
  following owner: <code>root</code>'
ocil_clause: /etc/shadow- has owner root
oval_external_content: null
platform: null
prodtype: all
rationale: 'The <tt>/etc/shadow-</tt> file is a backup file of <tt>/etc/shadow</tt>,
  and as such,

  it contains the list of local system accounts and password hashes.

  Protection of this file is critical for system security.'
references: {cis: 6.1.7}
requires: []
severity: medium
template:
  backends: {}
  name: file_owner
  vars: {filepath: /etc/shadow-, fileuid: '0', missing_file_pass: 'true'}
title: Verify Group Who Owns Backup shadow File
warnings: []
