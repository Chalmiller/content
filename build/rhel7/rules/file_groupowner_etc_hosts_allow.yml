conflicts: []
description: '

  To properly set the group owner of <code>/etc/hosts.allow</code>, run the command:

  <pre>$ sudo chgrp root /etc/hosts.allow</pre>'
identifiers: {cce: CCE-83824-3}
inherited_platforms: [machine]
ocil: 'To check the group ownership of <code>/etc/hosts.allow</code>, run the command:

  <pre>$ ls -lL /etc/hosts.allow</pre>

  If properly configured, the output should indicate the following group-owner:

  <code>root</code>'
ocil_clause: /etc/hosts.allow has group owner root
oval_external_content: null
platform: machine
prodtype: ol7,rhel7
rationale: 'The <tt>/etc/hosts.allow</tt> file is used to control access of clients
  to daemons in the

  server. Insecure groupownership of this file could allow users to grant clients
  unrestricted

  access or no access at all to services in the server.'
references: {cis: 3.4.4}
requires: []
severity: medium
template:
  backends: {}
  name: file_groupowner
  vars: {filegid: '0', filepath: /etc/hosts.allow, missing_file_pass: 'true'}
title: Verify Group Ownership of /etc/hosts.allow
warnings: []
