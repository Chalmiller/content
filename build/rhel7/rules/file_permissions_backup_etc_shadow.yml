conflicts: []
description: '

  To properly set the permissions of <code>/etc/shadow-</code>, run the command:

  <pre>$ sudo chmod 0000 /etc/shadow-</pre>'
identifiers: {cce: CCE-83416-8}
inherited_platforms: [null]
ocil: 'To check the permissions of <code>/etc/shadow-</code>, run the command:

  <pre>$ ls -l /etc/shadow-</pre>

  If properly configured, the output should indicate the following permissions:

  <code>----------</code>'
ocil_clause: /etc/shadow- has unix mode ----------
oval_external_content: null
platform: null
prodtype: all
rationale: 'The <tt>/etc/shadow-</tt> file is a backup file of <tt>/etc/shadow</tt>,
  and as such,

  it contains the list of local system accounts and password hashes.

  Protection of this file is critical for system security.'
references: {cis: 6.1.7}
requires: []
severity: medium
template:
  backends: {}
  name: file_permissions
  vars: {filemode: '0000', filepath: /etc/shadow-, missing_file_pass: 'true'}
title: Verify Permissions on Backup shadow File
warnings: []
