conflicts: []
description: ' To properly set the group owner of <code>/etc/passwd-</code>, run the
  command: <pre>$ sudo chgrp root /etc/passwd-</pre>'
identifiers: {cce: CCE-83323-6}
inherited_platforms: [null]
ocil: 'To check the group ownership of <code>/etc/passwd-</code>, run the command:
  <pre>$ ls -lL /etc/passwd-</pre> If properly configured, the output should indicate
  the following group-owner: <code>root</code>'
ocil_clause: /etc/passwd- has group owner root
oval_external_content: null
platform: null
prodtype: all
rationale: 'The <tt>/etc/passwd-</tt> file is a backup file of <tt>/etc/passwd</tt>,
  and as such,

  it contains information about the users that are configured on the system.

  Protection of this file is critical for system security.'
references: {cis: 6.1.6}
requires: []
severity: medium
template:
  backends: {}
  name: file_groupowner
  vars: {filegid: '0', filepath: /etc/passwd-, missing_file_pass: 'true'}
title: Verify Group Who Owns Backup passwd File
warnings: []
