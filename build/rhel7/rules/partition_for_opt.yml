conflicts: []
description: 'It is recommended that the <tt>/opt</tt> directory resides on a separate

  partition.'
identifiers: {cce: CCE-83339-2}
inherited_platforms: [null]
ocil: 'Run the following command to determine if <code>/opt</code>

  is on its own partition or logical volume:

  <pre>$ mount | grep "on /opt"</pre>

  If <code>/opt</code> has its own partition or volume group, a line will be returned.

  '
ocil_clause: no line is returned
oval_external_content: null
platform: machine
prodtype: fedora,ol7,ol8,rhel7,rhel8
rationale: 'The <tt>/opt</tt> partition contains additional software, usually installed

  outside the packaging system. Putting this directory on a separate partition

  makes it easier to apply restrictions e.g. through the <tt>nosuid</tt> mount

  option.'
references: {anssi: BP28(R12)}
requires: []
severity: medium
template:
  backends: {}
  name: mount
  vars: {mountpoint: /opt}
title: Ensure /opt Located On Separate Partition
warnings: []
