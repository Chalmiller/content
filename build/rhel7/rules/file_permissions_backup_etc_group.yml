conflicts: []
description: '

  To properly set the permissions of <code>/etc/group-</code>, run the command:

  <pre>$ sudo chmod 0644 /etc/group-</pre>'
identifiers: {cce: CCE-83482-0}
inherited_platforms: [null]
ocil: 'To check the permissions of <code>/etc/passwd</code>, run the command:

  <pre>$ ls -l /etc/passwd</pre>

  If properly configured, the output should indicate the following permissions:

  <code>-rw-r--r--</code>'
ocil_clause: /etc/group- has unix mode -rw-r--r--
oval_external_content: null
platform: null
prodtype: all
rationale: 'The <tt>/etc/group-</tt> file is a backup file of <tt>/etc/group</tt>,
  and as such,

  it contains information regarding groups that are configured on the system.

  Protection of this file is important for system security.'
references: {cis: 6.1.8}
requires: []
severity: medium
template:
  backends: {}
  name: file_permissions
  vars: {filemode: '0644', filepath: /etc/group-, missing_file_pass: 'true'}
title: Verify Permissions on Backup group File
warnings: []
