# platform = multi_platform_all
# complexity = low
# reboot = false
# disruption = low
# strategy = disable
- name: Ensure freeradius is removed
  package:
    name: freeradius
    state: absent
  tags:
    - CCE-82751-9
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_freeradius_removed
